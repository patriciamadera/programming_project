//const { DataTypes } = require("sequelize");
//const { sequelize } = require("../config/db");

//const CategorySQL = sequelize.define("CategorySQL", {
  //id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true },
 // name: { type: DataTypes.STRING, allowNull: false, unique: true }, // Nombre único
 // description: { type: DataTypes.TEXT, allowNull: true }            // Descripción opcional
//});

//sequelize.sync();
//module.exports = CategorySQL;
